<%= form_with(model: architect) do |form| %>
  <% if architect.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(architect.errors.count, "error") %> prohibited this architect from being saved:</h2>

      <ul>
        <% architect.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
